#pragma once
/*
#include "common.h"
#include "Pracownik.h"

const int ONE_DAY_TIME = 420;

struct ilosc_pracownikow
{
	int ilosc_monter = -1;
	int ilosc_spawacz = -1;
};

ilosc_pracownikow licz_zapotrzebowanie(const vector<operacja>& procLL_V1, const vector<operacja>& procLL_V2, const vector<operacja>& procRL_V1, const vector<operacja>& procRL_V2, const vector<zamowienie>& zamowienia);

void zapotrzebowanie_proces(const vector<operacja>& proc, float& spawacz, float& monter,
	const zamowienie& zam);

vector<Pracownik> stworz_pracownikow(ilosc_pracownikow wyliczeni_pracownicy);

vector<vector<int>> wczytaj_bazowy_harmonogram();*/
