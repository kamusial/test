#include "stdafx.h"
#include "functions.h"
/*
void licz_zapotrzebowanie(vector<operacja>& procLL_V1,
	vector<operacja>& procLL_V2,
	vector<operacja>& procRL_V1,
	vector<operacja>& procRL_V2,
	vector<zamowienie>& zam)
{
	float czas_spawacz = 0;
	float czas_monter = 0;
	float ilosc_monter = 0;
	float ilosc_spawacz = 0;



	for (int i = 0; i < sizeof(procLL_V1); ++i)
	{
		if (procLL_V1[i].ilosc_spawacz != 0)
		{
			czas_spawacz += procLL_V1[i].ilosc_spawacz * procLL_V1[i].czas
		};


	}



};*/