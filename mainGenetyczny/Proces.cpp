#include "Proces.h"
#include "stdafx.h"

/*void Proces::wczytaj()
{
	std::ifstream dane_csv;
	dane_csv.open(nazwa_proces".csv", ios::in); //jak wpisaæ, aby otworzy³ plik z nazw¹= nazwa_proces.scv? Osobny plik dla ka¿degbo obiektu klasy.
	if (!dane_csv.is_open())
	{
		cout << "plik dane_proces1 nie istnieje" << endl;
		exit(0);
	}

	short int shtmp1, shtmp2;
	string sttmp1, sttmp2;
	double dtmp1,
	bool btmp1;
	char ctmp;
	//dane_csv >> str1; //1sza linia
	for (int i = 0; i < 9; i++)
	{
		dane_csv >> shtmp1 >> ctmp >> sttmp1>>ctmp>>sttmp2>>ctmp>>dtmp1>>ctmp>>shtmp2>>ctmp>>btmp1;//sk¹d wie, ¿eby zacz¹æ drug¹ liniê? //tutaj ma ci¹giem tekst itp, jak zrobiæ, ¿eby idealnie rozpozna³ separator?
		Proces worker(stmp1; stmp2); // nazwa obiektu zale¿na od wczytanej wartoœci?
	}

	Proces::Proces(string a, short int b, struct c[x], unsigned short int d)
	{
		nazwa_proces = a;
		id_proces = b;
		proces_details = c; //zrobiæ strukture
		ilosc stanowisk = d;
	}
}
*/